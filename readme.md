# 🚀 OpenVPN Access Server (Cracked Edition)

**Контейнер для быстрого развёртывания OpenVPN Access Server с предустановленной лицензией**  
> Основан на образе [`ghcr.io/alexflyme/openvpn-as-cracked:latest`]

---

## 🧩 Возможности

- 🔒 Полнофункциональный OpenVPN Access Server  
- ⚙️ Простая установка в один шаг  
- ♻️ Автоматический перезапуск контейнера при сбоях  
- 💾 Сохранение всех данных и настроек в локальную директорию (`./data`)  
- 🌐 Веб-интерфейс управления и подключение через VPN  

---

## 🧱 Требования

Перед установкой убедитесь, что у вас установлены:

- **Docker** — [инструкция по установке](https://docs.docker.com/get-docker/)
- Поддержка **TUN** устройства в ядре Linux (`/dev/net/tun`)
- Порты **943**, **443** и **1194 (UDP)** свободны
- Права для запуска контейнеров и доступа к `/dev/net/tun`

---

## ⚡ Быстрый старт

Запустите контейнер одной командой:

```bash
docker run -d \
  --name=openvpn-as \
  --device /dev/net/tun \
  --cap-add=MKNOD --cap-add=NET_ADMIN \
  -p 943:943 -p 443:443 -p 1194:1194/udp \
  -v ./data:/openvpn \
  --restart=unless-stopped \
  ghcr.io/alexflyme/openvpn-as-cracked:latest

