# 🚀 OpenVPN Access Server (Cracked Edition)

**Контейнер для быстрого развёртывания OpenVPN Access Server с предустановленной лицензией**  
> Основан на образе [`ghcr.io/alexflyme/openvpn-as-cracked:latest`]

---

## 🧩 Возможности

- 🔒 Полнофункциональный OpenVPN Access Server  
- ⚙️ Простая установка в один шаг  
- ♻️ Автоматический перезапуск контейнера при сбоях  
- 💾 Сохранение всех данных и настроек в локальную директорию (`./data`)  
- 🌐 Веб-интерфейс управления и подключение через VPN  

---

## 🧱 Требования

Перед установкой убедитесь, что у вас установлены:

- **Docker** — https://docs.docker.com/get-docker/
- Поддержка **TUN** устройства в ядре Linux (`/dev/net/tun`)
- Порты **943**, **443** и **1194 (UDP)** свободны
- Права для запуска контейнеров и доступа к `/dev/net/tun`

---

## ⚡ Быстрый старт

Запустите контейнер одной командой:

```bash
docker run -d \
  --name=openvpn-as-cracked \
  --device /dev/net/tun \
  --cap-add=MKNOD --cap-add=NET_ADMIN \
  -p 943:943 -p 443:443 -p 1194:1194/udp \
  -v ./data:/openvpn \
  --restart=unless-stopped \
  ghcr.io/alexflyme/openvpn-as-cracked:latest
```

---

## 📂 Структура данных

| Путь в контейнере | Локальный путь | Назначение |
|--------------------|----------------|-------------|
| `/openvpn`         | `./data`       | Конфигурация, сертификаты и ключи OpenVPN Access Server |
| `/openvpn/log`     | `./data/log`   | Логи сервера (если образ их сохраняет в `/openvpn/log`) |
| `/openvpn/config`  | `./data/config`| Основные конфигурационные файлы сервера |

> ⚠️ Конкретная структура внутри `/openvpn` может немного отличаться в зависимости от версии образа.  
> В любом случае всё необходимое для восстановления хранится в `./data`.

---

## 🌍 Доступ к серверу

После запуска контейнера:

- **Веб-интерфейс администратора:**  
  🔗 `https://<IP_сервера>:943/admin`

- **Панель пользователя / подключение:**  
  🔗 `https://<IP_сервера>:943/`

Замените `<IP_сервера>` на IP-адрес вашего хоста или домен.

**Учётные данные по умолчанию:**

```
Username: openvpn
Password: <указывается в логах контейнера или при первом запуске>
```

> 💡 Если образ автоматически создаёт учётную запись — смотрите логи контейнера.  
> Если нет — создайте пользователя вручную через админку.

---

## 🪄 Просмотр логов и пароля

Чтобы просмотреть логи контейнера и, при необходимости, найти автоматически сгенерированный пароль:

```bash
docker logs openvpn-as-cracked 2>&1 | grep -i password
```

Если используете **docker-compose**:

```bash
docker compose logs openvpn-as-cracked 2>&1 | grep -i password
```

---

## 🧰 Управление контейнером

| Команда | Действие |
|----------|-----------|
| `docker ps` | Проверить, работает ли контейнер |
| `docker logs openvpn-as-cracked` | Просмотреть логи контейнера |
| `docker stop openvpn-as-cracked` | Остановить контейнер |
| `docker start openvpn-as-cracked` | Запустить контейнер снова |
| `docker restart openvpn-as-cracked` | Перезапустить контейнер |
| `docker rm -f openvpn-as-cracked` | Удалить контейнер |
| `docker pull ghcr.io/alexflyme/openvpn-as-cracked:latest` | Скачать обновлённый образ |

---

## 🧱 Пример `docker-compose.yml`

Создайте файл `docker-compose.yml` в директории проекта:

```yaml
version: '3.8'

services:
  openvpn-as-cracked:
    image: ghcr.io/alexflyme/openvpn-as-cracked:latest
    container_name: openvpn-as-cracked
    cap_add:
      - NET_ADMIN
      - MKNOD
    devices:
      - /dev/net/tun
    ports:
      - "943:943"
      - "443:443"
      - "1194:1194/udp"
    volumes:
      - ./data:/openvpn
    restart: unless-stopped
```

**Запуск:**

```bash
docker compose up -d
```

**Остановка и удаление контейнера (без удаления данных):**

```bash
docker compose down
```

---

## ♻️ Обновление контейнера до последней версии

**1. Скачать новый образ:**

```bash
docker pull ghcr.io/alexflyme/openvpn-as-cracked:latest
```

**2. Остановить и удалить старый контейнер:**

```bash
docker stop openvpn-as-cracked
docker rm openvpn-as-cracked
```

**3. Запустить новый контейнер (командой `docker run` или `docker compose up -d`):**

```bash
docker compose up -d
```

**Если используете `docker-compose`:**

```bash
docker compose pull
docker compose up -d
```

---

## 🛠️ Технические замечания и частые проблемы

**🧩 `/dev/net/tun` отсутствует**  
Убедитесь, что модуль `tun` загружен:

```bash
sudo modprobe tun
ls -la /dev/net/tun
```

---

**⚙️ Порты заняты**  
Проверьте, что порты 943/443/1194 свободны:

```bash
sudo ss -tuln | grep -E "943|443|1194"
```

---

**📂 Проблемы с правами на папку `./data`**  
Выставьте корректные права/владельца:

```bash
mkdir -p ./data
sudo chown $(id -u):$(id -g) ./data
```

---

## ❓ FAQ

**Q:** Нужно ли регистрировать лицензию?  
**A:** Нет, пропатчено до 1368 лицензий.

---

**Q:** Как добавить пользователя?  
**A:** Через веб-интерфейс администратора (`/admin`).

---

**Q:** Как экспортировать конфигурацию клиента?  
**A:** Через панель пользователя в веб-интерфейсе можно скачать профиль или конфигурационный файл клиента.

---

## 🧾 Лицензия и ответственность

Этот проект и образ предоставляются **только для образовательных целей** и был реализован
для ознакомления.
Использование образа с изменённой или взломанной лицензией может нарушать законы.  
Все действия вы совершаете **на свой страх и риск**.

---

## ❤️ Автор

**AlexFlyme**  
📦 GitHub Packages: [ghcr.io/alexflyme/openvpn-as-cracked](https://ghcr.io/alexflyme/openvpn-as-cracked)

---
